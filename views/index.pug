//- extends layout

//- block content
//-   h1= title
//-   p  Rehana Naguru wishes you Happy Halloween

//-   button(type="button", class="btn btn-success", onclick="update()") Press Me
//-   br
//-   br
//-   div(id = "target") 
//-     b Display 
//-     br
//-   br  
//-   a(href = '/nuts') Nuts Page
//-   script.  
//-    var client_count=0;
//-      function update() {
//-        client_count++;
//-        document.getElementById("target").innerHTML=`Value now ${client_count}`;
//-      }

extends layout
block content
 if (!user)
  a(href="/login") Login
  br
  a(href="/register") Register
 if (user)
  p You are currently logged in as #{user.username}
  a(href="/costumes") Take me to costumes
  br
  a(href="/logout") Logout